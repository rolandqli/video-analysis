# Meta requirements - install per-service deps into separate venvs
# Run: ./scripts/setup_venvs.sh
#
# Service-specific:
#   requirements/video-analysis.txt  -> .venv-video-analysis
#   requirements/sam2-segmentation.txt -> .venv-sam2-segmentation
#   requirements/test.txt -> for running tests (pytest)
